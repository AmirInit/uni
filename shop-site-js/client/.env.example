# ------------------------------------------------------------------
# The client works with NO .env file at all.
#
# By default the frontend calls "/api/..." and the Vite dev server proxies
# those requests to http://localhost:5000 (see vite.config.js). Only set this
# if you deploy the API to a different host.
# ------------------------------------------------------------------

# VITE_API_URL=http://localhost:5000/api
